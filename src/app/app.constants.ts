import { environment } from "../environments/environment";

export const DEFAULT_LOCALE = environment.defaultLocale;
export const USE_BROWSER_LANGUAGE = environment.useBrowserLanguage;
export const LANGUAGES: string[] = environment.activeLanguages;
export const LANGUAGE_META = environment.languageMeta;

export const POTENTIAL_CUSTOMER_TYPE = '2';
export const ACTIVE_USER_TYPE = '1';
export const ACTIVE_USER_TYPE2 = 'null';
