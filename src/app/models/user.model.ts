export class User {
  constructor(
    public id: number,
    public firstName: string,
    public lastName: string,
    public msisdn: string,
    public email: string,
    public imageUrl: string,
  ) {
  }
}
